apiVersion: v1
kind: Service
metadata:
  name: pathfinder
  namespace: pathfinder
  labels:
    app: pathfinder
spec:
  type: ClusterIP
  ports:
    - port: 9545
      targetPort: 9545
      protocol: TCP
      name: rpc
  selector:
    app: pathfinder
