---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: pathfinder
  namespace: argocd
  labels:
    app: pathfinder
spec:
  project: default
  source:
    repoURL: https://github.com/t-p/PiCluster
    targetRevision: main
    path: apps/pathfinder
    directory:
      recurse: true
  destination:
    server: https://kubernetes.default.svc
    namespace: pathfinder
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=false
      - PrunePropagationPolicy=foreground
      - PruneLast=true
    retry:
      limit: 3
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m
  revisionHistoryLimit: 10
  info:
    - name: "Description"
      value: "Pathfinder Starknet full node for self-custody RPC"
    - name: "Documentation"
      value: "https://github.com/eqlabs/pathfinder"
